"""One module per controllable load.

Each is a port of the integration that used to own that appliance —
`dhwp_smart` for the water heater, `pool_pump` for the pump — kept as close to
the original as the corrections allow, because those files encode incidents
rather than opinions. Every deviation from the source is marked ``ROUTEUR:``.
"""
